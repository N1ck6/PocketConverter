# Runtime dependencies (what gets bundled into converter.exe)
pillow==12.3.0
pillow-heif==1.8.0
PyMuPDF==1.28.2
python-docx==1.2.0
pdf2docx==0.5.13
fpdf2==2.8.8
pyyaml==6.0.3
markdown==3.10.3
html2text==2025.4.15
beautifulsoup4==4.15.0
imageio-ffmpeg==0.6.0
win11toast==0.36.3
# Transitive (via pdf2docx) but bundled into the exe: pinned so a rebuild
# can't silently pick up a version the PyInstaller hooks don't support yet
numpy==2.5.3
fonttools==4.66.0
opencv-python-headless==5.0.0.93
